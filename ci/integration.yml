---
platform: linux
image_resource:
  type: docker-image
  source:
    repository: alpine
    tag: "3.4"
inputs:
  - name: repo
run:
  path: sh
  args:
    - -e
    - -c
    - |
      echo "The integration tests are running on GitHub actions."
